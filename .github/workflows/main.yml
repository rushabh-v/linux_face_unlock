name: CI

on: [push]

jobs:
  build:

    runs-on: [ubuntu-18.04, ubuntu-16.04]

    steps:
    - uses: actions/checkout@v1
    - name: Run a multi-line script
      run: |
        chmod +x install.sh
        sudo ./install.sh
