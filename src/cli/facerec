
# Autocomple for the facerec command on cli

_facerec()
{
    local options="new enable disable remove --help"
    local current_word="${COMP_WORDS[COMP_CWORD]}"
    COMPREPLY=($(compgen -W "${options}" -- "$current_word"))
}
complete -F _facerec facerec
